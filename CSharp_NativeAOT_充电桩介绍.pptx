封面
- 标题：C# + NativeAOT 在单片机上的应用
- 副标题：充电桩项目实战（STM32H723）
- 团队 / 日期

项目概述
- 将 C# 与 NativeAOT 引入 MCU 平台，运行业务逻辑与第三方协议
- 目标：提高开发效率、加速迭代、保留高性能外设驱动

核心方案
- 业务：用 C# 编写并通过 NativeAOT 编译为本地二进制
- 驱动：现有 C 驱动（LCD、SPI、CAN、串口、SDIO）暴露给 C#
- 支持丰富 NuGet 库（MQTT、TLS、gRPC、LVGL Sharp 等，可免费使用）

主要优势
- 开发效率高、维护成本低
- 能直接复用成熟生态与协议实现
- 性能与体积可控，适合复杂业务场景

充电桩项目亮点
- 芯片：STM32H723（Cortex-M7）
- 已实现：国标协议（GBT27930）、MQTT/TLS、GUI、本地网页、日志与诊断
- 最终二进制：约 24 MB（包含驱动与第三方库）

第三方库与许可
- 列举（示例）：MQTT、TLS/证书、log4net、gRPC、GBT27930、LVGL Sharp
- 强调：这些库均可从 NuGet 免费获取（交付前会做许可证确认）

双向互操作：C# 调用 C，C 调用 C#
- 驱动层保持 C 实现以保证实时性与中断处理
- C# 通过已定义的 C 接口控制外设与业务逻辑（明确分层，易维护）
- C 层可通过回调/函数指针向 C# 报事件或通知（实现事件驱动、异步处理）
- 价值：兼顾实时性与高层开发效率，支持复杂协议与用户交互场景

外设与集成
- 外设：LCD、SPI、CAN、串口、SDIO（使用现有 C 驱动）
- 方案：驱动保留 C 实现，业务由 C# 控制，接口明确、分工清晰

维护与调试能力
- 设备上运行轻量网页服务器：展示设备信息、充电状态与日志，便于现场维护与调试

硬件需求与部署
- 最小硬件：ARM M7（如 H7）、SD 卡 或 NOR FLASH（支持 XIP 更佳）、SDRAM
- 未来计划：支持内存更小的 M4 平台（需裁剪）

适用场景
- 复杂协议、多业务逻辑产品：充电桩、网关、工业控制等
- 不适合极端受限设备或对微妙实时性要求的路径

项目成果与价值
- 缩短开发周期、降低维护成本
- 在目标硬件上验证可运行完整协议与 UI
- 方便现场维护，提高产品可用性

总结与下一步
- 推荐在新一代复杂嵌入式产品中试点此方案
- 可安排演示（实机跑通）、交付依赖清单与部署方案
- 联系人 / 团队信息